# 启动服务
```javascript
'use strict' var http = require('http')
 http.createServer((request, response) => {   // 发送 HTTP 头部,指定状态码和内容类型   response.writeHead(200, {'Content-Type': 'text/plain'});    // 发送响应数据   response.end('Hello World\n') }).listen(8888)
```
[x] 哈哈



